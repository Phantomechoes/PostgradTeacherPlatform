# backend/app/services

**职责：业务规则。**

以后这里放 Service。当 API 收到一个请求后，真正决定“允不允许、怎么处理、结果是什么”的代码在这一层。

例如以后的规则可能是：一个师资档案怎样才算完整、查询院校时如何过滤。  
这些规则写在 Service，而不是写在 Router 或数据库访问代码里。

调研尚未确认的规则（支付、佣金、选师、SLA 等）不得在这里写死。
