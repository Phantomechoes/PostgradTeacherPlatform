# backend/app/api

**职责：接收外部请求，调用 Service，返回 HTTP 响应。**

以后这里放 FastAPI 的路由（Router）。例如健康检查 `GET /health`、后续的院校查询接口，都会先进入这一层。

这一层应该做的事：

- 校验请求参数
- 调用对应的 Service
- 把结果或错误转换成 HTTP 响应

这一层不应该做的事：

- 直接写复杂业务规则
- 直接读写数据库
- 把数据库内部字段原样暴露出去

业务前缀约定（S0-02 起生效）：`/api/v1`。健康检查：`GET /health`。
